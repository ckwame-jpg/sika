pandas>=2.2
pyarrow>=18.0
numpy>=1.26
scikit-learn>=1.6
joblib>=1.4
xgboost>=2.1
httpx>=0.28
pydantic>=2.10
sqlalchemy>=2.0
psycopg[binary]>=3.2
pytest>=8.0
# Bug #29 — shared train/serve feature spec. Relative path resolves
# from this file's directory, so ``pip install -r apps/ml/requirements.txt``
# from any cwd picks up ``packages/ml-features/`` at the repo root.
-e ../../packages/ml-features
